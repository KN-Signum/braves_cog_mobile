name: braves_cog
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+3

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  lucide_icons_flutter: ^3.1.9
  
  # State management
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  
  # HTTP client
  http: ^1.2.0
  
  # Utilities
  shared_preferences: ^2.2.2
  
  # SVG support
  flutter_svg: ^2.0.10
  cognition_package:
    git:
      url: https://github.com/Adidadi0112/cognition_package.git
      ref: main
  
  # URL Launcher
  url_launcher: ^6.2.0
  
  # Path Provider - for accessing app documents directory
  path_provider: ^2.1.0
  
  # Google Fonts - Space Grotesk & Inter
  google_fonts: ^6.1.0
  flutter_dotenv: ^6.0.0
  equatable: ^2.0.8
  dartz: ^0.10.1
  intl: ^0.20.2
  research_package:
    git: 
      url: https://github.com/Adidadi0112/research.package.git
      ref: master

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_launcher_icons: ^0.13.1
  flutter_native_splash: ^2.4.0

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  
  # Code generation
  build_runner: ^2.4.8
  riverpod_generator: ^2.4.0
  
  # Testing
  mockito: ^5.4.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - .env
    - assets/images/
    - assets/icons/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Flutter Launcher Icons configuration
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/braves_logo.png"
  adaptive_icon_background: "#0F2847"
  adaptive_icon_foreground: "assets/images/braves_logo_adaptive.png"
  min_sdk_android: 21
  # Remove the default padding for adaptive icons to prevent zoomed appearance
  remove_alpha_ios: true

# Flutter Native Splash configuration
flutter_native_splash:
  color: "#FFFFFF"
  color_dark: "#0F2847"
  image: "assets/images/braves_logo_splash.png"
  android: true
  ios: true
  web: false
  android_gravity: center
  fullscreen: false
  
  android_12:
    image: "assets/images/braves_logo_splash.png"
    # Brak kolorów - użyje systemowego tła
    branding_mode: disabled


dependency_overrides:
  research_package:
    git: 
      url: https://github.com/Adidadi0112/research.package.git
      ref: master
